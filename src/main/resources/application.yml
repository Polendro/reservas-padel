spring:
  application:
    name: padel-booking
  datasource:
    url: jdbc:mysql://localhost:3306/padel_booking
    username: padel
    password: padel
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
